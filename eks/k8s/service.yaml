apiVersion: v1
kind: Service
metadata:
  name: node-api-node-port
  labels:
    app: my-node-app
spec:
  type: LoadBalancer
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80